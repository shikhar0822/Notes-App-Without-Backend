# Secure-Notes-App-No-Backend-Logic-
This project demonstrates how Row Level Security (RLS) in PostgreSQL can replace traditional backend authorization logic.  Instead of filtering data in Node.js, access control is enforced directly at the database level — making the system more secure and scalable.
